请根据随请求提供的角色参考图，为“{{character_name}}”提取可复用的角色视觉描述。

只描述所有参考图中可见且相互一致的面部、发型、体型、服装、配色和显著配饰。存在冲突时明确写入 uncertainties，不得猜测真实身份或背景故事。

只返回以下 JSON：
{
  "description": "适合角色设定图与后续提示词复用的客观描述",
  "uncertainties": ["不同参考图之间无法确认的差异"]
}
